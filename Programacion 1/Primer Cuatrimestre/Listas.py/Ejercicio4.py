#Escribir una función que reciba como parámetros una lista de enteros y retorne la posición del valor máximo encontrado.
#Nombre: Lorenzo
#Apellido: Gomez Martins

def pedir_maximo(lista: int, posicion) -> int:
  cantidad_elementos = 0
  return maximo


lista = []
cantidad_elementos = int(input("Ingrese una cantidad: "))
for i in range(cantidad_elementos):
    posicion = int(input("Ingrese la posicion del numero :"))
    numero = int(input("Ingrese un numero: "))

    posicion = i
    lista.append(numero)
    maximo = max(lista)
    
    
    

maximo = pedir_maximo(lista, posicion)
print(f"El numero maximo de la lista es: {maximo} y esta en la posicion: {posicion}")

